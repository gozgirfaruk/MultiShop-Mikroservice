@{
    ViewData["Title"] = "ProductDetail";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- Breadcrumb Start -->
<div class="container-fluid">
    <div class="row px-xl-5">
        <div class="col-12">
            <nav class="breadcrumb bg-light mb-30">
                <a class="breadcrumb-item text-dark" href="#">Ana Sayfa</a>
                <a class="breadcrumb-item text-dark" href="#">Alışveriş</a>
                <span class="breadcrumb-item active">Ürün Detay</span>
            </nav>
        </div>
    </div>
</div>
<!-- Breadcrumb End -->
<!-- Shop Detail Start -->
<div class="container-fluid pb-5">
    <div class="row px-xl-5">
        @await Component.InvokeAsync("_ProductDetailImageViewPartial", new { id = ViewBag.x })

        @await Component.InvokeAsync("_ProductDetailBodyViewPartial", new { id = ViewBag.x })

    </div>


    <div class="row px-xl-5">
        <div class="col">
            <div class="bg-light p-30">
                <div class="nav nav-tabs mb-4">
                    <a class="nav-item nav-link text-dark active" data-toggle="tab" href="#tab-pane-1">Açıklama</a>
                    <a class="nav-item nav-link text-dark" data-toggle="tab" href="#tab-pane-2">Ürün Hakkında</a>
                    <a class="nav-item nav-link text-dark" data-toggle="tab" href="#tab-pane-3">Yorumlar</a>
                </div>
                <div class="tab-content">
                    @await Component.InvokeAsync("_ProductDetailDescriptionViewPartial", new { id = ViewBag.x })

                    @await Component.InvokeAsync("_ProductDetailInfoViewPartial", new { id = ViewBag.x })

                   
                    <div class="tab-pane fade" id="tab-pane-3">
                        <div class="row">
                            @await Component.InvokeAsync("_ProductDetailCommentViewPartial", new { id = ViewBag.x })
                            @await Html.PartialAsync("/Views/Product/AddComment.cshtml")
                           
                        </div>
                    </div>
                  
                </div>
            </div>
        </div>
    </div>
</div>
<!-- Shop Detail End -->
